// src/testing/setup.ts
import '@testing-library/jest-dom/vitest'; // Erweitert Vitests `expect` um DOM-spezifische Matcher (toBeInTheDocument, etc.)

// Hier könntest du auch globale Mocks oder andere Setups hinzufügen,
// die für alle Tests gelten sollen.
