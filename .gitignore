# === Dependencies ===
# Lockfiles should be committed (package-lock.json, yarn.lock, pnpm-lock.yaml)
# Ignore dependency directories
node_modules/
.pnp
.pnp.*

# === Next.js Specific ===
# Build output
.next/
# Static export output
out/

# === Testing Frameworks ===
# Playwright generated reports and artifacts
playwright-report/
test-results/
# Vitest browser mode output (if used)
.vitest-browser/
# Jest/Vitest coverage reports
coverage/
*.lcov

# === Certificates ===
# Locally trusted development certificates generated by mkcert or similar
# Should not be committed to the repository!
*.pem

# === Logs ===
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Node.js diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# === Runtime Data ===
pids/
*.pid
*.seed
*.pid.lock

# === Caches ===
# General Caches
.cache/
.parcel-cache
# TypeScript build info
*.tsbuildinfo
# ESLint cache
.eslintcache
# Stylelint cache
.stylelintcache
# Other specific caches if used...

# === OS / Editor Specific ===
# macOS
.DS_Store
# VSCode
.vscode/
# JetBrains IDEs
.idea/
# Vim swap files
*.swp

# === Environment Variables ===
# Ignore all .env files by default to prevent committing secrets
.env*
# BUT, allow example or CI configuration files (uncomment if you use them)
# !.env.example
# !.env.ci

# === Local Database Files ===
# Ignore SQLite or other local-only database files
*.db
*.db-journal

# === Misc ===
# Output of 'npm pack'
*.tgz
# Vercel deployment output
.vercel
# Terraform state files (if used in project)
# .terraform/
# *.tfstate
# *.tfstate.*

# === Generated Type Definitions ===
# Usually not committed, but depends on project setup
# next-env.d.ts